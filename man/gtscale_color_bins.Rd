% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtscale_color_bins.R
\name{gtscale_color_bins}
\alias{gtscale_color_bins}
\title{Add only a binned color legend to a \code{gt} table}
\usage{
gtscale_color_bins(
  data,
  column = NULL,
  palette,
  domain = NULL,
  bins,
  labels = NULL,
  title = NULL,
  width = "180px",
  height = "14px"
)
}
\arguments{
\item{data}{A \code{gt_tbl} created by \code{\link[gt:gt]{gt::gt()}}.}

\item{column}{An optional numeric column in the underlying table used to infer
\code{domain}.}

\item{palette}{A vector of colors or palette endpoints used to color the bins.}

\item{domain}{A numeric vector of length 2 giving the scale limits. When
omitted, the limits are inferred from \code{column}.}

\item{bins}{A numeric vector of bin boundaries.}

\item{labels}{A labeling function or a character vector for the bins. When a
function is supplied, it is applied to the bin boundaries before interval
labels are constructed.}

\item{title}{Optional legend title.}

\item{width}{Width of the legend.}

\item{height}{Height of the swatches.}
}
\value{
A modified \link[gt:gt]{gt::gt} table.
}
\description{
This is a lower-level helper for cases where table coloring is already
handled elsewhere. For the usual "color and legendize" workflow, prefer
\code{\link[=gtscale_data_color_bins]{gtscale_data_color_bins()}}.
}
\examples{
library(gt)

exibble |>
  gt() |>
  data_color(
    columns = currency,
    method = "bin",
    palette = c("#f7fbff", "#08306b"),
    bins = c(0, 10, 100, 1000, 10000000)
  ) |>
  gtscale_color_bins(
    column = currency,
    palette = c("#f7fbff", "#08306b"),
    bins = c(0, 10, 100, 1000, 10000000),
    title = "Binned values"
  )
}
